{
  "name": "{xblock-name}-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "_build": "vite build",
    "build:student": "COMPONENT=student-ui npm run _build",
    "build:studio": "COMPONENT=studio-ui npm run _build",
    "build": "npm run build:student && npm run build:studio",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "@openedx/paragon": "22.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "typescript": "^5.3.0",
    "@types/react": "^17.0.80",
    "@types/react-dom": "^17.0.25",
    "sass": "^1.69.0"
  },
  "comments": {
    "versions": {
      "react": "ARCHITECTURAL: MUST be 17.x for OpenEdX compatibility",
      "paragon": "ARCHITECTURAL: MUST be 22.x to match OpenEdX version",
      "reason": "OpenEdX uses React 16.14.0. React 17 is compatible, React 18 is not. Paragon components work without IntlProvider."
    }
  }
}
